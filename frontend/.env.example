VITE_API_URL=http://localhost:8000

# Same Google Client ID as the backend's GOOGLE_CLIENT_ID (public value,
# safe to expose to the browser — it's an identifier, not a secret).
# Leave blank to hide the Google Sign-In button entirely.
VITE_GOOGLE_CLIENT_ID=
